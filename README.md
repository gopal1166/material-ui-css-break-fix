# Material-ui css fix in React

Use this component inside the App.js   to solve the below css break

```
use JssProvider from 'react-jss/lib/JssProvider'
```

Css break:

![picture](material_ui_css_break.png)